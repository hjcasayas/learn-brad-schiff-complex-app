{
  "name": "@complex-app/web-express-ts-css",
  "version": "1.0.0",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc --build --clean && tsc --build && copy-files-from-to",
    "start": "node dist/index.js",
    "npm-check": "npm-check -u",
    "watch": "nodemon --exec \"npm run build\" -e ts,js,ejs --ignore dist/ --ignore node_modules/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^5.0.1"
  },
  "devDependencies": {
    "@types/ejs": "^3.1.5",
    "@types/express": "^5.0.0",
    "copy-files-from-to": "^3.12.0"
  },
  "copyFiles": [
    {
      "from": "src/views/**/*.ejs",
      "to": "dist/src/views/"
    }
  ],
  "copyFilesSettings": {
      "whenFileExists": "overwrite"
  }
}
